apiVersion: v1
kind: Secret
metadata:
  name: notehub-secret
  namespace: notehub
type: Opaque
# ⚠️  IMPORTANT: These values are BASE64-ENCODED DEFAULTS for local dev ONLY.
# NEVER commit real credentials to version control.
# Regenerate before ANY non-local deployment:
#   echo -n 'your_strong_secret' | base64
# Values below are base64 of the shown plaintext:
data:
  # plaintext: change_me_in_production
  JWT_SECRET: Y2hhbmdlX21lX2luX3Byb2R1Y3Rpb24=
  # plaintext: notehub_pass
  DB_PASSWORD: bm90ZWh1Yl9wYXNz
  # plaintext: root_pass
  MYSQL_ROOT_PASSWORD: cm9vdF9wYXNz
